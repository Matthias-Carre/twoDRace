Class Player {  
  constructor(color, life, points, x_axis, y_axis) {
    this.color = color;
    this.life = life;
    this.points = points;
    this.x_axis = x_axis;
    this.y_axis = y_axis;
  }
}

set_x_axis(posX){this.x_axis = posX;}
set_y_axis(posY){this.x_axis = posX;}
};
